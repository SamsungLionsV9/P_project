# ====================================
# 환경 변수 설정 파일 (예시)
# ====================================
# 이 파일을 프로젝트 루트에 .env로 복사한 후 값을 입력하세요
# cp setup/env.example .env

# ========== MySQL ==========
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=car_database
MYSQL_USER=root
MYSQL_PASSWORD=your_mysql_password

# ========== JWT ==========
# JWT 시크릿 키 (최소 256비트 BASE64 인코딩)
# 생성 방법: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_key_at_least_256_bits_base64_encoded

# ========== OAuth2 소셜 로그인 (선택) ==========

# Google OAuth2
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Naver OAuth2
NAVER_CLIENT_ID=your-naver-client-id
NAVER_CLIENT_SECRET=your-naver-client-secret

# Kakao OAuth2
KAKAO_CLIENT_ID=your-kakao-client-id
KAKAO_CLIENT_SECRET=your-kakao-client-secret

# ========== Groq AI (선택) ==========
# ML 서비스에서 AI 분석 기능 사용 시 필요
GROQ_API_KEY=your-groq-api-key

# ========== 서버 설정 ==========
# Spring Boot 포트
USER_SERVICE_PORT=8080

# FastAPI ML 서비스 포트
ML_SERVICE_PORT=8000

